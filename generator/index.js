const data = require("./data");
const contract = require("./contract");

console.log(data);
console.log(contract);

// delete folders inside ipfs
// create folders inside ipfs, including subdirectories
// create contract.json
// in a for loop, generate each image from data with attributes and set in ipfs/images the image and in
// ipfs/metadata the metadata, divided by type
// create a file with data.json with the md5 and other output

